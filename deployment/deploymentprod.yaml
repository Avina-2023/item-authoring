apiVersion: apps/v1
kind: Deployment
metadata:
  name: authoringuiservice
spec:
  replicas: 1
  selector:
    matchLabels:
      app: authoringuiservice
  template:
    metadata:
      labels:
        app: authoringuiservice
    spec:
      containers:
        - name: authoringuiservice
          image: uapprod.azurecr.io/authoringuiservice:$tag
          resources:
           limits:
             cpu: "1"
             memory: "4Gi"
           requests:
             cpu: "500m"
             memory: "2Gi"
          imagePullPolicy: Always
          env:
          - name: PORT
            value: '4007'
          ports:
          - containerPort: 4007
      imagePullSecrets:
        - name: mysecret
      nodeSelector:
        agentpool: uapuat